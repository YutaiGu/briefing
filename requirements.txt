# Dependencies live in pyproject.toml (single source of truth).
# Runtime:         pip install -r requirements.txt   (same as  pip install -e .)
# With build tool: pip install -e .[build]           (adds pyinstaller)
-e .
